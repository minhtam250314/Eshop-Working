import { Col, Image } from 'antd';
import styled from 'styled-components';

export const WrapperStyleImageSmall = styled(Image)`
    height: 64px;
    width: 64px;
`

export const WrapperStyleCalImage = styled(Col)`
    flexbasis: unset;
    display: flex;
`